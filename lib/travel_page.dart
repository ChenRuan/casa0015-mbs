import 'package:flutter/material.dart';

class TravelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('travels Page'),
      ),
      body: Center(
        child: Text('Welcome to the travel Page!'),
      ),
    );
  }
}
